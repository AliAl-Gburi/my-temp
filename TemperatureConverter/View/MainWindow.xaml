<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <LinearGradientBrush x:Key="brush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="Red" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <StackPanel>

        <TextBlock Text="Value in Celcius:" FontWeight="Bold" Padding="2" FontSize="16" Background="{StaticResource brush}" />



        <TextBox x:Name="textBoxC" TextAlignment="Center"/>
        <TextBlock Text="Value in Fahrenheit:" FontWeight="Bold" Padding="2" FontSize="16" Background="{StaticResource brush}" />

        <TextBox x:Name="textBoxF" TextAlignment="Center"/>
        <Button Content="Convert to Celsius" Click="ConvertToCelsius"/>
        <Button Content="Convert to Fahrenheit" Click="ConvertToFahrenheit"/>
    </StackPanel>
    
</Window>
